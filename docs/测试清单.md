# JChatMind 项目测试清单

> 基于代码实际分析与项目文档，覆盖 `backend_v2` 全部核心模块。
> 生成日期：2026-05-25

---

## 一、性能测试清单

### 1.1 数据库查询性能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.1.1 | pgvector 向量相似度检索延迟 | 验证 `similaritySearchDetailed`（`<->` L2 距离）在 chunk 数量 10K/50K/100K 场景下的查询延迟 | JMH 或 pgbench；针对 `chunk_bge_m3` 表；生产级数据量 | P0 | P95 检索延迟 < 200ms（单知识库、topK=3） |
| P-1.1.2 | 向量检索 + context filter 组合查询 | 验证 `similaritySearchDetailedWithContext` 带 `sourceName`/`sourceType`/`contentPathPrefix` 过滤的性能损耗 | EXPLAIN ANALYZE 对比有无 context filter 的查询计划 | P1 | 增加 context filter 后延迟增长不超过 50% |
| P-1.1.3 | 多路召回并发查询 | `retrieveWithPlan` 同时发起向量、标题精确/contains/keyword/trigram、BM25 查询时的数据库连接压力 | 模拟 10 并发检索请求；监控 `pg_stat_activity` | P1 | 10 并发下无连接池耗尽，各通道在超时内完成 |
| P-1.1.4 | 用户记忆向量检索 | `UserMemoryMapper.similaritySearch` 按用户维度 + embedding 距离排序的查询延迟 | 构造 1K 条用户记忆，重复检索 50 次取均值 | P2 | 单用户 1K 条记忆时 P95 < 100ms |
| P-1.1.5 | 批量知识库 ID 查询 | `kb_id IN (...)` 大批量 kbIds（5-20 个知识库）对查询计划的影响 | EXPLAIN ANALYZE | P2 | 能利用 kb_id 索引，无全表扫描 |
| P-1.1.6 | 聊天消息高并发写入 | `ChatMessageMapper.insert` 在高并发异步场景下的写入性能 | JMeter/wrk 模拟并发聊天请求 | P1 | 100 TPS 写入无显著延迟增长，无死锁 |

### 1.2 API 接口性能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.2.1 | 聊天消息创建接口响应时间 | `POST /api/chat-messages` 同步阶段耗时（持久化+事件发布，不含异步 Agent 执行） | JMeter/wrk；测量 TTFB | P0 | 同步阶段 < 100ms |
| P-1.2.2 | 聊天消息列表查询 | `GET /api/chat-messages/session/{sessionId}` 消息数 100/500/1000 时的响应时间 | JMeter；构造不同消息量的会话 | P1 | 1000 条历史消息查询 < 500ms |
| P-1.2.3 | Agent 列表查询 | `GET /api/agents` 并发查询 | JMeter | P2 | 50 并发时 P95 < 100ms |
| P-1.2.4 | 文档上传建库耗时 | `POST /api/documents/upload` 端到端耗时（解析+分块+embedding+入库） | 上传 10K/50K/200K 字符 Markdown；测量总耗时 | P0 | 50K 文档建库 < 30s |
| P-1.2.5 | Embedding 调用瓶颈分析 | 确认长文档处理慢的根因是 embedding 调用而非 Markdown 解析 | 分阶段计时：解析耗时 vs embedding 耗时 vs 入库耗时 | P0 | 输出各阶段耗时占比，定位瓶颈 |

### 1.3 Agent 执行链路性能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.3.1 | Think 阶段单次调用延迟 | `JChatMind.think()` 单次模型调用（含内存压缩、prompt 构建、toolCallbacks 注册）端到端延迟 | 打点计时 | P0 | 无工具调用场景 Think 延迟 < 模型 API 响应时间 + 200ms |
| P-1.3.2 | Execute 阶段工具执行总耗时 | `CompletableFuture.supplyAsync(...).orTimeout(30s)` 计时（已有 `elapsedMs` 日志） | 复用现有日志；JMH 微基准 | P0 | 工具执行超时阈值 30s 合理，超时自动终止并返回错误 |
| P-1.3.3 | 完整 Agent Loop 端到端耗时 | 从 `run()` 到 `FINISHED` 的总耗时 | 端到端打点；典型对话场景（无工具/单工具/多工具） | P0 | 简单对话 < 5s；多轮工具调用 < 60s（MAX_STEPS=20 兜底） |
| P-1.3.4 | Memory 压缩机制效率 | `compressMemoryIfNeeded()` 8K 字符阈值触发后 `generateSummary()` 的额外耗时 | 构造超长对话（>8000 字符上下文），测量压缩前后 Think 耗时差异 | P1 | 压缩操作增加延迟 < 3s |
| P-1.3.5 | 异步事件处理吞吐 | `AsyncConfig` 线程池（core=4, max=10, queue=100）连续处理 ChatEvent 的能力 | 模拟快速连续发送 20 条消息；监控线程池队列深度和拒绝次数 | P1 | 队列不溢出，无 `TaskRejectedException` |

### 1.4 RAG 链路性能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.4.1 | Embedding 接口调用延迟（单次） | `RagServiceImpl.doEmbed()` 通过 WebClient 调 Ollama `/api/embed` 的单次延迟 | JMH；启用/禁用缓存分别测试 | P0 | 单次 < 500ms；缓存命中 < 1ms |
| P-1.4.2 | Embedding 缓存命中率 | LRU 缓存（默认 2048 条目）实际命中率 | 记录缓存大小和命中/未命中计数；典型业务流量 | P1 | 典型 session 内缓存命中率 > 60% |
| P-1.4.3 | 多路召回 + RRF 融合延迟 | 全通道（vector + 5 种标题通道 + BM25 title + BM25 content）并发/串行执行总延迟 | 典型 query 场景，记录各 channel 耗时及 RRF 融合耗时 | P0 | 全通道召回 + RRF + rerank 总延迟 < 2s |
| P-1.4.4 | Rerank 计算延迟 | `rerank()` 对 30-60 候选 chunk 进行 lexical scoring + retrieval signals 计算 | 关闭/开启 rerank 的 A/B 对比 | P1 | rerank 增加延迟 < 100ms（候选数 < 60） |
| P-1.4.5 | BM25 全文候选召回延迟 | 知识库含 5K+ chunk 时 tokenize + BM25 计算耗时 | 测量 `findContentBm25Candidates` 和 `findTitleBm25Candidates` 延迟 | P2 | BM25 计算延迟 < 500ms（候选 < 10K） |

### 1.5 SSE 推送延迟与吞吐量

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.5.1 | SSE 消息推送延迟 | 消息产生到前端 `EventSource` 接收的延迟 | 在 `refreshPendingMessages()` 和前端打点对比时间戳 | P0 | 本地环境延迟 < 100ms |
| P-1.5.2 | SSE 并发连接数上限 | `ConcurrentHashMap<String, SseEmitter>` 管理会话级连接 | JMeter 建立 50/100/200 个 SSE 长连接 | P1 | 200 并发连接服务端 CPU/内存无异常 |
| P-1.5.3 | 高频消息推送稳定性 | Agent 多步执行连续推送多条 SSE 消息（PLANNING -> THINKING -> EXECUTING -> ... -> DONE） | 模拟多轮工具调用，观察前端是否消息丢失或乱序 | P0 | 无消息丢失，消息顺序正确 |

### 1.6 内存与资源占用

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| P-1.6.1 | Agent 运行时内存占用 | 单 Agent 完整运行时（含 chatMemory、pendingChatMessages、ToolCallbacks）的内存 footprint | JProfiler/VisualVM；heap dump | P1 | 单 Agent 内存 < 50MB（不含模型响应） |
| P-1.6.2 | Embedding 缓存内存占用 | 2048 条目 LRU 缓存（每条约 1024 维 × 4 bytes ≈ 4KB）的总内存 | 计算理论值 + JVM 实测 | P2 | 缓存 < 16MB |
| P-1.6.3 | SSE 连接泄漏检测 | `onCompletion`/`onTimeout`/`onError` 回调是否正确清理 `ConcurrentHashMap` | 长时间运行，模拟客户端异常断开；定期检查 `clients.size()` | P0 | 24 小时运行后无孤儿 emitter |
| P-1.6.4 | 异步线程池资源回收 | `AsyncConfig` 线程池空闲时段是否正确回收线程 | 监控线程数随时间变化 | P2 | 空闲 60s 后线程数回落至 corePoolSize 附近 |

---

## 二、功能测试清单

### 2.1 Agent 编排功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.1.1 | Agent 状态机流转 | `AgentState` 六态（IDLE -> PLANNING -> THINKING -> EXECUTING -> FINISHED/ERROR）正常流转 | 单元测试；构造 JChatMind，mock ChatClient 返回无 toolCalls 响应 | P0 | 六种状态按预期顺序流转，IDLE 状态下重复 `run()` 抛 `IllegalStateException` |
| F-2.1.2 | Think-Execute 循环正常收敛 | 单一工具调用场景：Think->Execute->Think（第二轮无 toolCalls）->FINISHED | 单元测试；mock ChatClient 第一次返回 toolCalls，第二次返回纯文本 | P0 | 两轮 `step()` 后状态为 FINISHED |
| F-2.1.3 | MAX_STEPS 兜底终止 | 模型持续返回 toolCalls 时 `MAX_STEPS=20` 强制终止 | 单元测试；mock ChatClient 始终返回 toolCalls | P0 | 第 20 轮后 agentState= FINISHED，日志 warn |
| F-2.1.4 | terminate 工具显式结束 | 模型调用 `terminate` 后 `execute()` 检测并设置 FINISHED | 单元测试；mock ChatClient 返回带 terminate 的 toolCalls | P0 | 检测到 terminate 后 agentState=FINISHED |
| F-2.1.5 | Agent 异常终止流 | `run()` 中发生未捕获异常时状态进入 ERROR 并抛出 RuntimeException | 单元测试；mock 组件在 run 期间抛异常 | P0 | agentState=ERROR，异常被重新抛出 |
| F-2.1.6 | 防止重复工具调用提醒 | `previousToolCallSignature` 在下轮 `buildThinkPrompt()` 中作为提醒注入 | 构造两轮相同 toolCalls，验证 prompt 包含上一轮签名提醒 | P1 | 第二轮 think prompt 包含重复调用警告 |
| F-2.1.7 | 对话记忆压缩 | `compressMemoryIfNeeded()` 字符 > 8000 触发，保留最近 8 条，生成摘要 | 构造超长对话，验证压缩前后消息数和摘要内容 | P1 | 压缩后中间消息被摘要替换 |
| F-2.1.8 | Agent 运行时上下文组装 | `JChatMindFactory.create()` 正确组装 userId/agentId/chatSessionId 对应的配置、工具、知识库、历史记忆 | 集成测试；预设配置和会话历史，验证各字段 | P0 | 返回的实例包含正确的 systemPrompt、allowedTools、allowedKbs、chatMemory |

### 2.2 工具调用功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.2.1 | 固定工具始终加载 | KnowledgeTools、TerminateTool 在所有 Agent 运行时都被加载 | 单元测试：`ToolFacadeServiceImpl.getFixedTools()` | P0 | 固定工具列表包含 KnowledgeTools 和 TerminateTool |
| F-2.2.2 | 可选工具按配置加载 | DataBaseTools、EmailTools 等仅当 Agent 的 `allowedTools` 包含对应工具名时才加载 | 单元测试：`JChatMindFactory.resolveRuntimeTools()` | P0 | allowedTools 为空时仅加载固定工具 |
| F-2.2.3 | KnowledgeTools 知识库隔离 | 传入的 kbIds 必须在 Agent 的 `allowedKbMap` 内才能被检索 | 单元测试；传入不在 allowedKbs 中的 kbId | P0 | 未授权 kbId 被过滤 |
| F-2.2.4 | KnowledgeTools session context 回写 | 检索后 top1 chunk 的 sourceType/sourceName/contentPath 回写到 session 的 retrievalContext | 单元测试；mock ragService 返回带 metadata 的结果 | P0 | chatSession 的 retrievalContext 被正确更新 |
| F-2.2.5 | DataBaseTools SELECT 只读限制 | 仅允许 `SELECT` 开头的 SQL | 单元测试（已有 `shouldRejectNonSelectSql`）；补充 CREATE TABLE/TRUNCATE 等边缘案例 | P0 | 非 SELECT 语句返回"仅支持 SELECT 查询" |
| F-2.2.6 | DataBaseTools 查询结果格式化 | SQL 结果正确格式化为 Markdown 表格 | 单元测试（已有 `shouldFormatQueryResultsAsTable`） | P0 | 表头正确，数据行含 NULL，空数据集显示"(无数据)" |
| F-2.2.7 | EmailTools 参数校验 | to/subject/content 为空/null、邮箱无 `@` 时拒绝 | 单元测试（已有 `EmailToolsTest` 完整测试） | P0 | 非法参数返回明确错误信息 |
| F-2.2.8 | EmailTools 异步发送 | `sendEmailAsync` 提交后立即返回，不阻塞工具调用 | 单元测试；验证返回"已提交发送"且 service 被调用 | P0 | 返回包含"邮件已提交发送"和收件人/主题 |
| F-2.2.9 | FileSystemTools 路径遍历防护 | `validateAndResolvePath()` 阻止 `../` 等路径遍历攻击 | 单元测试；传入 `../../../etc/passwd` | P0 | 抛出 SecurityException "路径遍历攻击被阻止" |
| F-2.2.10 | FileSystemTools 基础 CRUD | readFile/writeFile/appendToFile/listFiles/deleteFile/createDirectory | 单元测试；临时目录中执行 CRUD 验证 | P1 | 各操作在允许目录内正常执行 |
| F-2.2.11 | 工具执行超时处理 | `executeWithTimeout()` 30s 超时后返回错误 ToolResponseMessage | 单元测试；mock 一个执行超过 30s 的工具 | P0 | 30s 后返回包含"工具执行异常"的错误消息 |
| F-2.2.12 | 工具不存在时的错误处理 | `executeWithTimeout()` 异常捕获分支为每个 toolCall 生成错误响应 | 单元测试；mock `toolCallingManager` 抛异常 | P1 | 每个 toolCall 都返回对应错误描述 |
| F-2.2.13 | Harness 审批流拦截 | `HarnessRunner.beforeExecution()` 返回需审批时，推送 `TOOL_APPROVAL_REQUIRED` SSE 事件并等待审批 | 单元测试（已有 `HarnessRunnerTest`）；补充端到端审批流测试 | P1 | 审批通过后正常执行 |
| F-2.2.14 | Harness Circuit Breaker | 工具连续失败超过阈值后被熔断拦截 | 单元测试（已有 `CircuitBreakerTest`） | P1 | 熔断状态正确流转（CLOSED -> OPEN -> HALF_OPEN -> CLOSED） |

### 2.3 知识库检索功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.3.1 | Markdown 文档解析分块 | 按标题切分为 section，每个 section 生成一个 chunk | 单元测试（已有 `MarkdownParserServiceImplTest`） | P0 | contentPath 正确反映层级，sectionIndex 递增 |
| F-2.3.2 | Chunk Embedding 生成与存储 | `contentPath + title + content` 拼接后调 embedding 接口，存入 pgvector 列 | 集成测试；上传文档后查表验证 | P0 | 所有 chunk embedding 列非空，维度与模型一致 |
| F-2.3.3 | Chunk Metadata 完整性 | metadata JSON 包含 retrievableTitle/contentPath/sourceType/sourceName/sectionIndex 等 | 单元测试；验证 JSON 解析后字段完整 | P0 | 全部必要字段存在且非空 |
| F-2.3.4 | 向量召回基本功能 | `similaritySearchDetailed` 按 L2 距离排序返回 topK chunk | 集成测试（已有 `RagRecallEvaluationTest` 离线评测基线） | P0 | 语义相关 chunk 排在前面 |
| F-2.3.5 | 标题精确匹配召回 | `searchByTitleExact` 经 normalize 后精确匹配 | 已有离线评测基线验证 | P0 | 标题精确匹配场景下 gold chunk 被召回 |
| F-2.3.6 | Path-Aware Query 召回 | 输入含 `>` 分隔符的路径式 query，触发自动路径选择 | 单元测试（已有 `shouldAutoSelectParentPathForPathAwareQueryAcrossKbs`） | P0 | 自动选择正确 parent contentPath，contextApplyMode=HARD |
| F-2.3.7 | Session Retrieval Context 多轮追问 | 第一轮检索后持久化上下文，第二轮裸 query 带 context 精确收敛 | 集成测试（已有 `RagSessionOnlineE2eEvaluationTest`） | P0 | 带 context 追问 Recall@5 显著高于无 context |
| F-2.3.8 | 多知识库检索隔离 | 仅检索 `effectiveKbIds` 中的知识库 | 单元测试；传入不包含的 kbId，验证结果过滤 | P0 | 返回结果全部属于传入的知识库 |
| F-2.3.9 | RRF 融合多通道结果 | `rrfFuse()` 将多通道结果按 RRF 算法合并排序（k=60） | 单元测试；构造重叠 chunk，验证 RRF 分数和去重 | P1 | 相同 chunkId 结果合并，RRF 分数正确累加 |
| F-2.3.10 | Rerank 重排序效果 | `rerank()` 综合 lexical、BM25、vector 信号重新排序 | A/B 对比开关 rerank 的 Recall@1 和 MRR | P1 | 开启 rerank 后 MRR 不下降 |
| F-2.3.11 | Query 改写意图识别 | 正确区分 FOLLOW_UP / NAVIGATION / FACTOID / ANALYTICAL 意图 | 单元测试（已有 `QueryRewriteServiceImplTest` 多场景覆盖） | P0 | 各意图场景分类正确 |
| F-2.3.12 | 知识库 CRUD 接口 | KnowledgeBaseController 创建/查询/更新/删除 | API 测试 | P1 | 创建返回 ID；更新后字段变更；删除后不可查 |
| F-2.3.13 | 文档 CRUD 接口 | DocumentController 上传/查询/更新/删除 | API 测试；上传后查询 chunk 表 | P1 | 上传后 chunk 入库；删除文档后 chunk 级联删除 |

### 2.4 消息持久化功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.4.1 | 用户消息创建触发异步 Agent | `createChatMessage()` 持久化用户消息后发布 `ChatEvent` | 单元测试；verify `publisher.publishEvent` 被调用 | P0 | 用户消息持久化成功后 ChatEvent 被发布 |
| F-2.4.2 | AI 消息持久化 | `saveMessage()` 将 AssistantMessage（含 toolCalls metadata）写入 `chat_message` 表 | 集成测试；Agent 执行后查库验证 | P0 | Assistant 消息 metadata 包含正确的 toolCalls JSON |
| F-2.4.3 | Tool 消息持久化 | `saveMessage()` 将 ToolResponseMessage 逐条写入 `chat_message` 表 | 集成测试；Agent 执行 tools 后查库验证 | P0 | 每个工具调用对应一条 role=TOOL 的消息 |
| F-2.4.4 | 会话历史恢复 | `JChatMindFactory.loadMemory()` 从数据库读取最近消息，恢复 System/User/Assistant/Tool 消息 | 单元测试；构造包含各种 role 消息的会话数据 | P0 | Assistant 消息恢复 toolCalls；Tool 消息恢复 toolResponse |
| F-2.4.5 | 消息所有权校验 | `requireOwnedSession()` 确保操作用户是会话所有者 | 单元测试；用 userId-A 操作 userId-B 的会话 | P0 | 跨用户访问抛出 BizException |
| F-2.4.6 | 消息追加接口 | `appendChatMessage()` 在已有消息后追加 content | 单元测试 | P2 | 原始 content + appendContent 拼接正确 |
| F-2.4.7 | 会话 CRUD 接口 | ChatSessionController 创建/查询/更新/删除会话 | API 测试 | P1 | 创建返回 sessionId；查询按 userId 过滤；删除后不可查 |

### 2.5 SSE 推送功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.5.1 | SSE 连接建立 | `SseController.connect()` 返回 SseEmitter 并发送 "init" 事件 | 集成测试；EventSource 连接 `/sse/connect/{chatSessionId}` | P0 | 前端收到 `name="init" data="connected"` 事件 |
| F-2.5.2 | Agent 状态 SSE 推送 | PLANNING/THINKING/EXECUTING/DONE 各阶段推送状态消息 | 集成测试；验证收到 AI_PLANNING/AI_THINKING/AI_EXECUTING/AI_DONE | P0 | 事件 type 和 statusText 匹配当前阶段 |
| F-2.5.3 | AI 生成内容 SSE 推送 | `refreshPendingMessages()` 通过 SSE 推送 AI/Tool 消息 | 集成测试；验证 type=AI_GENERATED_CONTENT，payload 完整 | P0 | 每条 AI/Tool 消息产生一条 SSE 事件 |
| F-2.5.4 | SSE 超时处理 | 30 分钟超时后触发 `onTimeout` 回调移除客户端 | 单元测试（已有 `SseServiceImplTest`） | P1 | 超时后 ConcurrentHashMap 中对应 sessionId 被移除 |
| F-2.5.5 | SSE 发送失败降级 | `send()` 中 IOException 时移除客户端并 log warn | 单元测试（已有 `shouldRemoveClientWhenSendingFails`） | P0 | IOException 后客户端被移除，不影响其他会话 |
| F-2.5.6 | SSE 未连接时不抛异常 | `send()` 找不到 emitter 时仅 log debug | 单元测试（已有 `shouldIgnoreSendWhenClientIsNotConnected`） | P0 | `assertDoesNotThrow` |
| F-2.5.7 | Harness 审批 SSE 推送 | `sendApprovalEvents()` 推送 TOOL_APPROVAL_REQUIRED 事件 | 集成测试；触发审批后验证前端收到审批事件 | P1 | 字段完整（approvalRequestId/toolName/toolInput/expiresAt 等） |

### 2.6 模型接入功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.6.1 | 多模型注册 | `MultiChatClientConfig` 注册 deepseek-chat 和 glm-4.6 两个 Bean | 单元测试；验证 `ChatClientRegistry` 包含两个 key | P0 | `registry.get("deepseek-chat")` 和 `registry.get("glm-4.6")` 非 null |
| F-2.6.2 | 按 Agent 配置切换模型 | `JChatMindFactory.buildAgentRuntime()` 根据 `agent.getModel()` 取对应 ChatClient | 集成测试；创建两个 Agent 分别使用不同 model | P0 | 模型不存在时抛 `IllegalStateException` |
| F-2.6.3 | ChatClientRegistry fallback | `UserMemoryFacadeServiceImpl.resolveChatClient()` 在找不到指定 client 时遍历 fallback | 单元测试；构造只含非指定 key 的 registry | P1 | fallback 返回第一个可用 ChatClient |

### 2.7 用户记忆功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.7.1 | LLM 记忆提取 | `extractWithLlm()` 从用户消息提取五类记忆（BACKGROUND/PREFERENCE/CONSTRAINT/GOAL/FACT） | 单元测试（已有 `UserMemoryFacadeServiceImplTest`）；补充正常提取路径 | P0 | LLM 返回正确 JSON 时成功解析 |
| F-2.7.2 | 关键词 fallback 提取 | LLM 不可用时 `extractWithKeywords()` 基于关键词分级 | 单元测试；验证 "记住"/"喜欢"/"我是" 等关键词分类 | P0 | LLM 为 null 时自动降级，type 分类正确 |
| F-2.7.3 | 记忆候选持久化 | `persistAutomatically()` 先插入候选（PENDING），再根据 shouldPersist 决定是否插入正式记忆 | 单元测试 | P0 | should_persist=true + importance=high/medium 时状态变 PERSISTED |
| F-2.7.4 | should_persist=false 丢弃 | 仅记录候选并标记 DISCARDED，不入正式记忆 | 单元测试 | P1 | candidate status=DISCARDED，userMemoryMapper.insert 不被调用 |
| F-2.7.5 | 冲突更新处理 | `"更新："` 前缀触发 `handleConflictUpdate()`：删除同 type 旧记忆，插入新记忆 | 单元测试 | P0 | 旧记忆被删除，新记忆入库 |
| F-2.7.6 | 重复记忆去重 | `findConfirmedMemoryByContent` 和 `findMemoryCandidateByContent` 防重复插入 | 单元测试；两次提取相同内容 | P0 | 第二次提取不产生新记录 |
| F-2.7.7 | 记忆向量化 Embedding | `generateEmbedding()` 调 `ragService.embed()` 生成 embedding | 单元测试 | P1 | embedding 维度与模型一致 |
| F-2.7.8 | 语义记忆召回 | `recallRelevantMemories()` 先 pgvector 召回，不足 topK 时用无 embedding 记忆补齐 | 单元测试（已有 `shouldFallbackToRecentMemoriesWhenSemanticRecallFails`） | P0 | 语义召回按 L2 距离排序，fallback 正常兜底 |
| F-2.7.9 | 记忆召回全路径故障兜底 | 所有路径失败时返回空列表，不抛异常 | 单元测试（已有 `shouldReturnEmptyWhenAllRecallFallbackPathsFail`） | P0 | 返回 emptyList |
| F-2.7.10 | 记忆 CRUD 接口 | UserMemoryController 查询/候选/删除 | API 测试（已有 `UserMemoryControllerTest`） | P1 | GET 返回正确数据；DELETE 后不可查 |
| F-2.7.11 | 长期记忆注入 Agent 上下文 | `loadLongTermMemory()` 格式化为 SystemMessage 注入 | 集成测试；有记忆用户发消息，验证 chatMemory 含记忆条目 | P0 | chatMemory 含 `[memoryType] content` 格式条目 |

### 2.8 MCP 服务端功能

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| F-2.8.1 | MCP API Key 鉴权 | `McpApiKeyFilter` 校验 `X-API-Key` 请求头 | API 测试：无 key / 错 key / 正确 key | P0 | 正确 key -> 200；错误/无 key -> 401 |
| F-2.8.2 | MCP API Key 未配置时放行 | `mcp.api-key` 为空时直接放行 | 配置空 api-key，访问 `/mcp/*` | P1 | 直接通过 |
| F-2.8.3 | MCP 工具提供者注册 | `mcpToolCallbackProvider` Bean 注册 McpKnowledgeTool、EmailTools、DataBaseTools | 单元测试；验证 provider 包含这三个工具 | P0 | `getToolCallbacks()` 数量 >= 3 |
| F-2.8.4 | McpKnowledgeTool 基本检索 | 参数校验（query/kbIds 非空）、检索结果格式化 | 单元测试；mock RagService | P1 | query/kbIds 为空返回错误；正常检索返回格式化文本 |
| F-2.8.5 | 外部工具 Provider 去重 | `buildExternalToolCallbacks()` 排除与本地工具同名的外部工具 | 单元测试；构造 local 和 external 重名工具 | P1 | external 与 local 重名时不重复添加 |

---

## 三、安全测试清单

### 3.1 输入校验

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| S-3.1.1 | userId 空值校验 | `requireUserId()` 拒绝空/null userId，抛出 BizException | 单元测试；传入 null/空字符串 | P0 | 抛出 BizException "userId 不能为空" |
| S-3.1.2 | SQL 注入防护—DataBaseTools | 仅接受 SELECT，但非参数化查询存在风险 | 安全测试；传入 `SELECT * FROM users WHERE name = 'admin' OR '1'='1'` | P0 | 确认是否需要升级为参数化查询 |
| S-3.1.3 | 路径遍历防护—FileSystemTools | `validateAndResolvePath()` 检查 `resolvedPath.startsWith(basePath)` | 单元测试；传入 `..\..\windows\system32\config\sam`（Windows） | P0 | 抛出 SecurityException |
| S-3.1.4 | Markdown 文件类型校验 | `DocumentController.uploadDocument()` 校验上传文件类型 | API 测试；上传非 .md 文件（.exe/.js） | P1 | 非 Markdown 文件被拒绝或正确处理 |
| S-3.1.5 | 文件大小限制 | 上传文档的文件大小上限控制 | API 测试；上传 100MB 文件 | P1 | 超大文件被 Spring Boot multipart 配置拦截 |
| S-3.1.6 | API 请求参数边界值 | 各 Controller 对极端值（超长字符串、特殊字符、null）的防护 | API 测试；各接口传入超长字符串（>10K） | P1 | 不崩溃，返回合理错误信息 |
| S-3.1.7 | XSS 防护—用户输入 | 用户发送 `<script>alert(1)</script>` 类内容 | API 测试；观察存储和前端展示 | P2 | 内容原样存储，前端需做 XSS 过滤 |

### 3.2 SQL 注入防护

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| S-3.2.1 | MyBatis 参数化查询 | 全部 Mapper XML 使用 `#{}` 而非 `${}` | 代码审查；扫描全部 9 个 Mapper XML | P0 | 用户输入相关参数全部使用 `#{}` |
| S-3.2.2 | kbIds 列表参数 | `<foreach>` + `CAST(#{kbId} AS uuid)` 不产生注入 | 代码审查 `ChunkBgeM3Mapper.xml` | P0 | foreach 内部使用 `#{}` 参数化 |
| S-3.2.3 | LIKE 查询注入 | `searchByTitleContains` 的 `containsPattern` 中 `%`/`_` 的行为确认 | 安全测试；query 含 `%` 字符 | P1 | 明确 `%` 是通配符还是字面值 |
| S-3.2.4 | pgvector 向量字面量 | `#{vectorLiteral}::vector` 由代码生成，不直接接受外部输入 | 代码审查 | P1 | 向量由 `float[]` 转换，无外部注入路径 |

### 3.3 权限控制

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| S-3.3.1 | 会话所有权校验 | `requireOwnedSession(userId, sessionId)` 验证会话归属 | API 测试；A 的 token 访问 B 的 session | P0 | 返回 BizException |
| S-3.3.2 | 知识库访问白名单 | Agent `allowedKbs` 限制可访问知识库 | 单元测试；allowedKbs=["kb-A"]，传入 kbIds=["kb-B"] | P0 | kb-B 被 `resolveEffectiveKbIds` 过滤 |
| S-3.3.3 | 工具访问白名单 | Agent `allowedTools` 限制可用可选工具 | 单元测试；allowedTools=[] 时 DataBaseTools 未加载 | P0 | 可选工具不在白名单时不加载 |
| S-3.3.4 | MCP API Key 鉴权 | `/mcp/*` 需要 `X-API-Key` 头 | 同 F-2.8.1 | P0 | 未授权访问返回 401 |
| S-3.3.5 | CORS 跨域限制 | `CorsConfig` 仅允许 `localhost:*` 和 `127.0.0.1:*` | API 测试；从其他 Origin 发起请求 | P1 | 非同源请求被拦截 |
| S-3.3.6 | 用户记忆隔离 | 记忆查询仅限 `userId` 匹配 | API 测试；跨用户操作 | P0 | 不返回其他用户数据 |

---

## 四、容错与异常处理测试清单

### 4.1 异常捕获与全局异常处理

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| E-4.1.1 | BizException 处理 | `GlobalExceptionHandler.handleBizException()` 包装为 `ApiResponse.error()` | 单元测试；触发 BizException | P0 | 返回 ApiResponse 格式，code=500 |
| E-4.1.2 | 通用 Exception 处理 | `handleException()` 返回"服务器内部错误"，日志记录完整堆栈 | 单元测试 | P0 | 不暴露内部异常细节 |
| E-4.1.3 | 404 处理 | `NoResourceFoundException` 返回 404 | API 测试；访问不存在路径 | P1 | 返回 404，不暴露内部信息 |
| E-4.1.4 | Async 超时处理 | `AsyncRequestTimeoutException` 返回 503 | 单元测试（已有 `shouldReturn503ForAsyncRequestTimeout`） | P1 | HttpStatus.SERVICE_UNAVAILABLE |
| E-4.1.5 | JSON 解析异常处理 | `ChatMessageConverter` 中 `JsonProcessingException` 包装为 BizException | 单元测试；构造非法 JSON metadata | P1 | 抛出 BizException 而非原始异常 |

### 4.2 服务降级与超时处理

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| E-4.2.1 | Ollama Embedding 不可用降级 | `recallRelevantMemories()` 中 `embed()` 失败时降级到按时间排序的 recent memories | 单元测试（已有 `shouldFallbackToRecentMemoriesWhenSemanticRecallFails`） | P0 | 语义召回失败不抛异常，返回 fallback 列表 |
| E-4.2.2 | LLM 记忆提取失败降级 | `extractWithLlm()` 异常时 fallback 到 `extractWithKeywords()` | 单元测试 | P0 | LLM 不可用时自动降级到关键词提取 |
| E-4.2.3 | 工具执行超时降级 | `executeWithTimeout()` 30s 超时后返回错误 ToolResponseMessage | 单元测试（同 F-2.2.11） | P0 | 超时工具返回错误信息，不影响其他工具 |
| E-4.2.4 | ChatClient 不存在时的处理 | `chatClientRegistry.get()` 返回 null 时抛 IllegalStateException | 单元测试；使用不存在 model 名 | P0 | 明确的错误信息 |
| E-4.2.5 | 长期记忆加载失败不阻塞 Agent | `loadLongTermMemory()` 异常时 catch 后 log warn 并返回 emptyList | 单元测试；mock service 抛异常 | P0 | Agent 继续正常执行，日志记录 warn |
| E-4.2.6 | Memory 压缩失败不阻塞 Think | `generateSummary()` 异常时 catch 后 log warn 跳过本次压缩 | 单元测试；mock chatClient 抛异常 | P0 | Think 继续正常执行 |
| E-4.2.7 | Agent run 异常后记忆提取继续 | `ChatEventListener.handle()` finally 块中 extraction 失败时 catch + log warn | 单元测试；mock extraction 失败 | P0 | Agent 结束后记忆提取失败不影响主流程 |
| E-4.2.8 | 记忆召回全故障兜底 | `recallRelevantMemories()` 所有路径失败时返回空列表 | 单元测试（已有 `shouldReturnEmptyWhenAllRecallFallbackPathsFail`） | P0 | 不抛异常 |

### 4.3 数据一致性

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| E-4.3.1 | 用户消息持久化失败不触发 Agent | `doCreateChatMessage()` 失败时应抛异常，不发布 ChatEvent | 单元测试；mock mapper.insert 返回 0 | P0 | 抛 BizException，ChatEvent 不发布 |
| E-4.3.2 | ChatMessage metadata JSON 序列化一致性 | AI 消息 toolCalls 和 Tool 消息 toolResponse 正确序列化/反序列化 | 单元测试；写入后读取验证 | P0 | toolCalls 和 toolResponse 在持久化后正确恢复 |
| E-4.3.3 | 记忆候选与正式记忆事务一致性 | candidate insert + confirmed memory insert 不在同一事务中，需确认是否需要 `@Transactional` | 代码审查 | P1 | 确认事务边界，评估风险 |
| E-4.3.4 | 会话消息删除级联 | 删除 ChatSession 时是否级联删除 ChatMessage？当前未发现级联删除 | 代码审查 | P2 | 明确行为：允许孤儿消息或补充级联删除 |

---

## 五、集成测试清单

### 5.1 端到端业务流程

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| I-5.1.1 | 用户发送消息 -> Agent 回复完整链路 | 前端发消息 -> 持久化 -> 异步启动 Agent -> Think -> 无工具 -> 生成回复 -> SSE 推送 -> 记忆提取 | 端到端集成测试（需完整 Spring 上下文+模型 API 或 mock 模型） | P0 | 整条链路完成，前端收到 AI_GENERATED_CONTENT |
| I-5.1.2 | Agent 使用 RAG 工具检索知识库 | 用户问知识库问题 -> Think 返回 KnowledgeTool call -> Execute 检索 -> 回填 -> 再 Think 生成答案 | 端到端测试（已有 `RagOnlineE2eEvaluationTest`） | P0 | 检索结果正确回填，答案基于检索内容 |
| I-5.1.3 | 多轮工具调用对话 | 用户问需同时用数据库和知识库的问题 -> Agent 先后调用两个工具 -> 综合回答 | 端到端测试 | P0 | 两个工具先后执行，最终答案综合两者结果 |
| I-5.1.4 | 会话历史恢复后继续对话 | 第一轮完成 -> 新请求同 session 的 ChatMessage -> Agent 基于历史回答 | 集成测试 | P0 | Agent 正确理解和引用历史对话 |
| I-5.1.5 | 长期记忆在后续对话中被引用 | 第一轮提取了偏好记忆 -> 第二轮 Agent 在 SystemMessage 中看到 -> 回答体现偏好 | 集成测试 | P1 | 第二轮回答中体现用户偏好 |
| I-5.1.6 | 完整知识库建库到检索流程 | 上传 Markdown -> 解析分块 -> embedding 入库 -> Agent 检索该知识库 -> 返回相关 chunk | 端到端测试（已有 `RealKnowledgeBaseImportTest`） | P0 | 完整链路跑通 |
| I-5.1.7 | 记忆提取到持久化到 Agent 上下文完整链路 | 用户对话 -> Agent 结束 -> 自动提取记忆 -> 入库 -> 下次会话恢复 -> 注入 prompt | 集成测试 | P1 | 跨会话记忆生效 |

### 5.2 外部依赖集成测试

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| I-5.2.1 | Ollama Embedding 服务连通性 | `doEmbed()` 调用 Ollama `/api/embed`（已有 `shouldCallOllamaEmbedEndpointWithInputAndKeepAlive`） | 集成测试 | P0 | 请求格式正确，返回 float[] |
| I-5.2.2 | DeepSeek ChatClient 连通性 | `deepSeekChatClient` 正常发起聊天请求 | 集成测试；发送简单 prompt | P0 | 返回非空 ChatResponse |
| I-5.2.3 | ZhiPuAI ChatClient 连通性 | `zhiPuAiChatClient` 正常发起聊天请求 | 集成测试 | P1 | 返回非空 ChatResponse |
| I-5.2.4 | PostgreSQL + pgvector 扩展可用 | pgvector extension 已安装，`<->` 运算符可用 | 集成测试；执行向量查询 | P0 | 返回正确的 L2 距离值 |
| I-5.2.5 | 邮件发送服务连通性 | `EmailServiceImpl.sendEmailAsync()` 可正常连接 QQ 邮箱 SMTP | 集成测试（已有 `EmailToolsTest`；需补 Service 层） | P2 | 邮件实际送达 |
| I-5.2.6 | TestController Health Check | 应用启动后 `/api/test` 返回正常 | Smoke test | P1 | 200 OK |

### 5.3 数据库集成测试

| 编号 | 测试项 | 测试目标 | 测试方法/工具 | 优先级 | 验收标准 |
|------|--------|----------|---------------|--------|----------|
| I-5.3.1 | MyBatis Mapper CRUD 集成 | 全部 9 个 Mapper 的 insert/select/update/delete 在真实数据库验证 | 集成测试；Testcontainers PostgreSQL + pgvector 或共享测试库 | P0 | 全部 CRUD 操作返回预期结果 |
| I-5.3.2 | pgvector 类型处理器 | `PgVectorTypeHandler` 正确处理 `float[]` 与 pgvector 双向转换 | 集成测试；插入带 embedding 实体后查询验证 | P0 | embedding 数据 round-trip 无损 |
| I-5.3.3 | JSONB 字段序列化 | Agent 的 allowedTools/allowedKbs/chatOptions 和 ChatMessage 的 metadata JSONB 序列化 | 集成测试；插入后读取验证 JSON 结构 | P0 | 序列化/反序列化后 List/Map 值正确 |
| I-5.3.4 | UUID 类型转换 | Mapper XML 中 `CAST(#{id} AS uuid)` 正确转换 | 集成测试；传入 String UUID | P0 | 无类型转换错误 |
| I-5.3.5 | pg_trgm 扩展可用 | `searchByTitleTrigram` 使用 `similarity()` 函数 | 集成测试 | P1 | 无 "function similarity does not exist" 错误 |
| I-5.3.6 | 数据库连接池配置 | HikariCP 默认连接池应对并发 Agent 执行 | 压力测试；10 并发 Agent | P1 | 无连接等待超时 |
| I-5.3.7 | Spring 上下文启动测试 | 应用启动时 Bean 装配成功 | `mvnw.cmd test` 中的 `JchatmindApplicationTests` | P0 | ApplicationContext 正常启动 |
| I-5.3.8 | 循环依赖检测 | 应用启动时无循环依赖 | 集成测试（已有 `CircularDependencyStartupTest`） | P0 | 无 `BeanCurrentlyInCreationException` |

---

## 六、已有测试覆盖与待补充

### 6.1 已有测试（27 个文件）

| 模块 | 已有测试类 | 覆盖程度 |
|------|-----------|----------|
| Agent/Harness | `HarnessRunnerTest`, `InMemoryApprovalStoreTest`, `CircuitBreakerTest`, `CircuitBreakerInterceptorTest`, `HarnessExecutionContextHolderTest`, `HarnessToolCallbackProxyTest`, `JChatMindFactoryExternalToolProviderTest`, `JChatMindV1Test`, `JChatMindV2Test` | 中（Harness 治理层较完整，Agent 核心循环缺失） |
| 工具 | `DataBaseToolsTest`, `EmailToolsTest`, `KnowledgeToolsTest`, `TerminateToolTest` | 中（参数校验覆盖好，集成场景少） |
| RAG | `MarkdownParserServiceImplTest`, `QueryRewriteServiceImplTest`, `RagServiceImplTest`, `RagRecallEvaluationTest`, `RagOnlineE2eEvaluationTest`, `RagSessionOnlineE2eEvaluationTest`, `RealKnowledgeBaseImportTest`, `MultiKnowledgeBaseRetrievalFlowTest` | 高（Query 改写和离线评测覆盖好，RRF/rerank 单元测试少） |
| 服务 | `SseServiceImplTest`, `UserMemoryFacadeServiceImplTest` | 低（仅部分降级场景） |
| 控制器 | `UserMemoryControllerTest` | 低（仅一个 Controller 有测试） |
| 基础 | `JchatmindApplicationTests`, `CircularDependencyStartupTest`, `GlobalExceptionHandlerTest` | 低（上下文启动当前失败） |

### 6.2 P0 优先补充测试

1. **JChatMind Think-Execute 循环核心流程**：状态机、MAX_STEPS、terminate、异常终止
2. **JChatMindFactory 运行时上下文组装**：配置反序列化、工具组装、知识库解析
3. **KnowledgeTools 知识库隔离**：`resolveEffectiveKbIds`、retrievalContext 管理
4. **FileSystemTools 路径遍历防护**：Windows 路径攻击向量
5. **ChatMessageFacadeServiceImpl 会话所有权校验**：跨用户访问防护
6. **McpApiKeyFilter API Key 鉴权**：无 key/错 key/正确 key 三条路径
7. **Controller 层集成测试**：至少覆盖 ChatMessageController、AgentController
8. **RagServiceImpl 多路召回 + RRF + Rerank 完整单元测试**
9. **数据库 Mapper 集成测试**：所有 Mapper 的真实数据库 CRUD 验证
10. **ApplicationContext 启动成功**：修复模型配置缺失问题

---

*清单基于 `backend_v2` 代码分析（截至 2026-05-25），覆盖 100+ Java 源文件和 9 个 Mapper XML。*
